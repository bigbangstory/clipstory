fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.18
psycopg[binary,pool]>=3.2,<4.0
itsdangerous>=2.2,<3.0
httpx>=0.27,<1.0

# Transcription. CTranslate2 backend; runs on CPU, ARM included.
faster-whisper>=1.1,<2.0

# Clip suggestions. Optional at runtime: without ANTHROPIC_API_KEY the feature
# switches itself off and everything else still works.
anthropic>=1.0,<2.0
pydantic>=2.9,<3.0
